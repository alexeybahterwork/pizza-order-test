import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  container: {
    flexDirection: 'column',
  },
  nextBtn: {
    width: 180,
  },
  backBtn: {
    textDecoration: 'none',
    width: 200,
  },
  noItemsBtn: {
    width: 200,
  },
  link: {
    textDecoration: 'none',
  },
});
