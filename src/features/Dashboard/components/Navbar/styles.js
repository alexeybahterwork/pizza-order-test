import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  container: {
    display: 'flex',
    justifyContent: 'space-between',
  },
  rightMenu: {
    display: 'flex',
    justifyContent: 'end',
    alignItems: 'center',
  },
});
